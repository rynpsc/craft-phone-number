{% import "_includes/forms" as forms %}

{{ forms.selectField({
    id: "defaultRegion",
    name: "defaultRegion",
    label: "Default Country"|t("phonenumber"),
    value: field.defaultRegion,
    options: field.getRegionOptions(),
    errors: field.getErrors("defaultRegion"),
    instructions: "The default country to use when parsing phone numbers."|t("phonenumber"),
}) }}

{{ forms.selectField({
    id: "previewFormat",
    name: "previewFormat",
    label: "Preview Format"|t("phonenumber"),
    value: field.previewFormat,
    options: field.getPreviewFormatSettingsOptions(),
    errors: field.getErrors("previewFormat"),
    instructions: "How field values will be formatted in element indexes and card views."|t("phonenumber"),
}) }}

